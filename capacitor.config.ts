import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'Trax.Flagship.Inspect',
  appName: 'Flagship.Inspect',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
